Stub — running shell commands on Claude Code lifecycle events (SessionStart, UserPromptSubmit, Stop, etc). The harness's escape hatch for behaviour memory can't enforce.

Use cases: [[Every PR]] gates, auto-format on stop, push notifications on long runs.

TODO.
