Stub — running a skill in a forked subagent so its tool output never enters the main window. The default for any skill that does noisy work (browser automation, log scanning, doc lookups).

See [[Forked Subagents]] for the underlying mechanic.

TODO.
