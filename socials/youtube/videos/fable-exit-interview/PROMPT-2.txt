Spawn an agent team for an exit interview: an Opus 4.8 agent (the incoming
consumer) interviews a Fable 5 agent (the departing veteran) to produce the
handover that lets Opus 4.8 work effectively on this codebase's real tasks.

Evidence base — the veteran answers ONLY from: my session transcript history,
the memory dir, git history, and the current code. The interviewer's job is to
cross-examine: any claim about paths/flags/behavior must be verified against
current main and stamped "verified <date>", or explicitly listed as unverified.
Derive the set of "real tasks" from transcript frequency and weight coverage
accordingly.

Scope — capture only tacit knowledge: sequencing, judgment calls, incident
history, where docs/memory have drifted. Do NOT restate anything already in
CLAUDE.md / AGENT-INSTRUCTIONS.md or already enforced by the harness (apply
the no-op test to every line). Where the memory dir's handover overlaps, this
document supersedes it — update the memory entry to point here.

Shape — a short orientation file plus disclosed reference files per concern;
pre-commit/pre-merge gates as short checkable checklists. Write the model-delta
guidance as "if you are a less capable model than the author," not hardcoded
to one successor.

Done means: every recurring session type from the transcripts has a playbook
entry; every claim stamped verified or flagged; files committed to the repo;
and a pointer added to CLAUDE.md so a fresh session actually discovers it.