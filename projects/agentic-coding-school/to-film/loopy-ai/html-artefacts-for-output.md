Stub — HTML artifacts as a workflow primitive. Three videos cluster around this: Interactive HTML Artifacts (the foundation), Channel HTML Artifacts (applied), Artifact Planning (the planning use).

Currently [[Prototypes as specs]] half-covers this but doesn't acknowledge the artifact-as-runtime version. Day 1 practice block demo.

TODO: write the framing that ties artifact-as-spec and artifact-as-output into one beat.
